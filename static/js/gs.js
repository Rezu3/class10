const gkQuestions = [
{
question: "1. বায়ুমণ্ডলের মোট ভরের প্রায় কত শতাংশ ট্রপোস্ফিয়ারে অবস্থান করে?",
options: ["50%", "60%", "75%", "90%"],
correctAnswer: 2
},
{
question: "2. ট্রপোস্ফিয়ারের গড় উচ্চতা কত?",
options: ["8 km", "12 km", "20 km", "50 km"],
correctAnswer: 1
},
{
question: "3. ল্যাপস রেটের গড় মান কত?",
options: ["4.5°C/km", "6.5°C/km", "8.5°C/km", "10°C/km"],
correctAnswer: 1
},
{
question: "4. বায়ুমণ্ডলের কোন স্তরে আবহাওয়া সংক্রান্ত অধিকাংশ ঘটনা ঘটে?",
options: ["স্ট্র্যাটোস্ফিয়ার", "মেসোস্ফিয়ার", "ট্রপোস্ফিয়ার", "থার্মোস্ফিয়ার"],
correctAnswer: 2
},
{
question: "5. ওজোন স্তর প্রধানত কোন স্তরে অবস্থিত?",
options: ["ট্রপোস্ফিয়ার", "স্ট্র্যাটোস্ফিয়ার", "মেসোস্ফিয়ার", "এক্সোস্ফিয়ার"],
correctAnswer: 1
},
{
question: "6. বাণিজ্যিক জেট বিমান সাধারণত কোন স্তরে উড়ে?",
options: ["ট্রপোস্ফিয়ার", "স্ট্র্যাটোস্ফিয়ার", "মেসোস্ফিয়ার", "থার্মোস্ফিয়ার"],
correctAnswer: 1
},
{
question: "7. বায়ুমণ্ডলের সবচেয়ে শীতল স্তর কোনটি?",
options: ["ট্রপোস্ফিয়ার", "স্ট্র্যাটোস্ফিয়ার", "মেসোস্ফিয়ার", "থার্মোস্ফিয়ার"],
correctAnswer: 2
},
{
question: "8. উল্কাপিণ্ড অধিকাংশ ক্ষেত্রে কোন স্তরে পুড়ে যায়?",
options: ["ট্রপোস্ফিয়ার", "স্ট্র্যাটোস্ফিয়ার", "মেসোস্ফিয়ার", "এক্সোস্ফিয়ার"],
correctAnswer: 2
},
{
question: "9. রেডিও তরঙ্গ প্রতিফলিত হয় প্রধানত কোন স্তর থেকে?",
options: ["ট্রপোস্ফিয়ার", "স্ট্র্যাটোস্ফিয়ার", "আয়নোস্ফিয়ার", "এক্সোস্ফিয়ার"],
correctAnswer: 2
},
{
question: "10. বায়ুমণ্ডলের সর্ববহিঃস্থ স্তর কোনটি?",
options: ["মেসোস্ফিয়ার", "থার্মোস্ফিয়ার", "ম্যাগনেটোস্ফিয়ার", "এক্সোস্ফিয়ার"],
correctAnswer: 3
},
{
question: "11. চ্যাপম্যান চক্রের সাথে সম্পর্কিত কোন গ্যাস?",
options: ["N₂", "CO₂", "O₂ ও O₃", "CH₄"],
correctAnswer: 2
},
{
question: "12. UV-C রশ্মি কোন অণুকে ভেঙে ওজোন গঠনে সাহায্য করে?",
options: ["CO₂", "N₂", "O₂", "CH₄"],
correctAnswer: 2
},
{
question: "13. ওজোন স্তরের প্রধান কাজ কী?",
options: ["তাপ উৎপাদন", "অতিবেগুনি রশ্মি শোষণ", "বৃষ্টি সৃষ্টি", "বায়ুচাপ বৃদ্ধি"],
correctAnswer: 1
},
{
question: "14. ওজোন ধ্বংসে সবচেয়ে গুরুত্বপূর্ণ ভূমিকা পালন করে কোন যৌগ?",
options: ["CO₂", "SO₂", "CFC", "NH₃"],
correctAnswer: 2
},
{
question: "15. CFC থেকে উৎপন্ন কোন মৌল ওজোন ধ্বংস করে?",
options: ["হাইড্রোজেন", "ক্লোরিন", "সোডিয়াম", "ক্যালসিয়াম"],
correctAnswer: 1
},
{
question: "16. ওজোন হোল সর্বাধিক দেখা যায় কোথায়?",
options: ["আর্কটিক", "আফ্রিকা", "অ্যান্টার্কটিকা", "ভারত"],
correctAnswer: 2
},
{
question: "17. NO ও NO₂ যৌথভাবে কী নামে পরিচিত?",
options: ["NOx", "SOx", "VOC", "CFC"],
correctAnswer: 0
},
{
question: "18. ওজোন স্তর ক্ষয়ের ফলে কোন রোগের ঝুঁকি বাড়ে?",
options: ["ম্যালেরিয়া", "ত্বকের ক্যান্সার", "টাইফয়েড", "ডায়াবেটিস"],
correctAnswer: 1
},
{
question: "19. অতিবেগুনি রশ্মির প্রভাবে চোখের কোন রোগ হতে পারে?",
options: ["গ্লুকোমা", "ছানি", "মায়োপিয়া", "অ্যাস্টিগম্যাটিজম"],
correctAnswer: 1
},
{
question: "20. মন্ট্রিয়ল প্রোটোকল মূলত কোন সমস্যা মোকাবিলার জন্য গৃহীত হয়?",
options: ["অ্যাসিড বৃষ্টি", "ওজোন ক্ষয়", "গ্লোবাল ওয়ার্মিং", "জলদূষণ"],
correctAnswer: 1
},
{
question: "21. গ্রিনহাউস প্রভাবের প্রধান কারণ কী?",
options: ["UV শোষণ", "IR শোষণ", "রেডিও তরঙ্গ", "X-ray"],
correctAnswer: 1
},
{
question: "22. প্রাকৃতিক গ্রিনহাউস প্রভাব না থাকলে পৃথিবীর গড় তাপমাত্রা কত হতো?",
options: ["0°C", "-18°C", "-40°C", "15°C"],
correctAnswer: 1
},
{
question: "23. সবচেয়ে গুরুত্বপূর্ণ মানবসৃষ্ট গ্রিনহাউস গ্যাস কোনটি?",
options: ["CH₄", "N₂O", "CO₂", "O₃"],
correctAnswer: 2
},
{
question: "24. মিথেনের উৎস কোনটি?",
options: ["ধানক্ষেত", "গবাদি পশু", "জলাভূমি", "উপরের সবকটি"],
correctAnswer: 3
},
{
question: "25. N₂O প্রধানত কোন উৎস থেকে নির্গত হয়?",
options: ["রাসায়নিক সার", "গাড়ি", "জাহাজ", "সৌরকোশ"],
correctAnswer: 0
},
{
question: "26. GWP-এর পূর্ণরূপ কী?",
options: ["Global Water Potential", "Global Warming Potential", "Green World Project", "Gas Warming Power"],
correctAnswer: 1
},
{
question: "27. গ্লোবাল ওয়ার্মিং-এর ফলে কী ঘটতে পারে?",
options: ["সমুদ্রপৃষ্ঠ বৃদ্ধি", "হিমবাহ গলন", "চরম আবহাওয়া", "সবকটি"],
correctAnswer: 3
},
{
question: "28. গ্লোবাল ওয়ার্মিং কমাতে কোনটি সবচেয়ে কার্যকর?",
options: ["বন উজাড়", "বৃক্ষরোপণ", "CFC ব্যবহার", "কয়লা ব্যবহার"],
correctAnswer: 1
},
{
question: "29. নবীকরণযোগ্য শক্তির উৎস কোনটি?",
options: ["কয়লা", "পেট্রোলিয়াম", "সৌরশক্তি", "প্রাকৃতিক গ্যাস"],
correctAnswer: 2
},
{
question: "30. জ্বালানির তাপন মূল্যের একক কী?",
options: ["J/kg", "kJ/kg", "W", "N"],
correctAnswer: 1
},
{
question: "31. সর্বাধিক ব্যবহৃত জীবাশ্ম জ্বালানি কোনটি?",
options: ["কয়লা", "পেট্রোলিয়াম", "কাঠ", "বায়োগ্যাস"],
correctAnswer: 1
},
{
question: "32. স্থিতিশীল উন্নয়নের মূল ধারণা কী?",
options: ["শুধু বর্তমানের উন্নয়ন", "ভবিষ্যৎ প্রজন্মের ক্ষতি না করে উন্নয়ন", "শিল্পায়ন", "শহরায়ন"],
correctAnswer: 1
},
{
question: "33. 'Fire Ice' বলতে কী বোঝায়?",
options: ["শুষ্ক বরফ", "মিথেন হাইড্রেট", "তরল নাইট্রোজেন", "জমাট CO₂"],
correctAnswer: 1
},
{
question: "34. মিথেন হাইড্রেট প্রধানত কোথায় পাওয়া যায়?",
options: ["মরুভূমি", "সমুদ্রতল", "পাহাড়", "নদী"],
correctAnswer: 1
},
{
question: "35. সৌরকোশ কোন প্রভাবের উপর কাজ করে?",
options: ["ফটোইলেকট্রিক", "ফোটোভোল্টায়িক", "সিজোইলেকট্রিক", "ডপলার"],
correctAnswer: 1
},
{
question: "36. সৌরকোশে সাধারণত কোন অর্ধপরিবাহী ব্যবহৃত হয়?",
options: ["তামা", "সিলিকন", "লোহা", "অ্যালুমিনিয়াম"],
correctAnswer: 1
},
{
question: "37. একাধিক সৌরকোশ যুক্ত হয়ে কী গঠন করে?",
options: ["সৌর ব্যাটারি", "সৌর প্যানেল", "ইনভার্টার", "ট্রান্সফরমার"],
correctAnswer: 1
},
{
question: "38. বায়ুকল কোন শক্তিকে বৈদ্যুতিক শক্তিতে রূপান্তর করে?",
options: ["সৌরশক্তি", "বায়ুশক্তি", "তাপশক্তি", "জোয়ার শক্তি"],
correctAnswer: 1
},
{
question: "39. Biomass-এর উদাহরণ কোনটি?",
options: ["গোবর", "কৃষি বর্জ্য", "কাঠ", "সবকটি"],
correctAnswer: 3
},
{
question: "40. বায়োগ্যাসের প্রধান উপাদান কী?",
options: ["CO₂", "H₂", "CH₄", "O₂"],
correctAnswer: 2
},
{
question: "41. বায়োগ্যাসে মিথেনের পরিমাণ প্রায় কত?",
options: ["20-30%", "40-50%", "55-70%", "80-90%"],
correctAnswer: 2
},
{
question: "42. CBM-এর পূর্ণরূপ কী?",
options: ["Coal Bed Methane", "Carbon Based Methane", "Coal Burning Method", "Compressed Bio Methane"],
correctAnswer: 0
},
{
question: "43. CBM কোথা থেকে সংগ্রহ করা হয়?",
options: ["সমুদ্র", "কয়লাস্তর", "বনভূমি", "আগ্নেয়গিরি"],
correctAnswer: 1
},
{
question: "44. জোয়ার-ভাটা শক্তির মূল কারণ কী?",
options: ["সূর্যের আলো", "চাঁদের মহাকর্ষ", "বাতাস", "ভূমিকম্প"],
correctAnswer: 1
},
{
question: "45. ভূ-উষ্ণতা শক্তির উৎস কী?",
options: ["সূর্য", "চাঁদ", "পৃথিবীর অভ্যন্তরের তাপ", "বায়ু"],
correctAnswer: 2
},
{
question: "46. কোন শক্তির উৎস আবহাওয়ার উপর নির্ভরশীল নয়?",
options: ["সৌরশক্তি", "বায়ুশক্তি", "ভূ-উষ্ণতা শক্তি", "জোয়ার শক্তি"],
correctAnswer: 2
},
{
question: "47. ম্যাগনেটোস্ফিয়ারের প্রধান কাজ কী?",
options: ["বৃষ্টি সৃষ্টি", "UV শোষণ", "সৌর বায়ু থেকে পৃথিবীকে রক্ষা", "তাপমাত্রা বৃদ্ধি"],
correctAnswer: 2
},
{
question: "48. থার্মোস্ফিয়ারে তাপমাত্রা বৃদ্ধির কারণ কী?",
options: ["ঘন বায়ু", "UV ও X-ray শোষণ", "বৃষ্টি", "মেঘ"],
correctAnswer: 1
},
{
question: "49. পৃথিবীর বায়ুমণ্ডলে সর্বাধিক পরিমাণে কোন গ্যাস রয়েছে?",
options: ["অক্সিজেন", "নাইট্রোজেন", "আর্গন", "কার্বন ডাই অক্সাইড"],
correctAnswer: 1
},
{
question: "50. ওজোন স্তর ধ্বংস ও গ্লোবাল ওয়ার্মিং-এর মধ্যে কোনটি সঠিক?",
options: [
"দুটিই একই ঘটনা",
"ওজোন ক্ষয় সরাসরি গ্লোবাল ওয়ার্মিং",
"দুটি পৃথক পরিবেশগত সমস্যা",
"কোনোটিই পরিবেশগত সমস্যা নয়"
],
correctAnswer: 2
}
];
































// gk.js - General Knowledge Quiz Logic

document.addEventListener('DOMContentLoaded', function() {

    // Quiz state variables
    let currentQuestionIndex = 0;
    let score = 0;
    let userAnswers = [];
    let quizTimer;
    let questionTimer;
    let quizStartTime;
    let quizCompleted = false;
    let autoAdvanceInterval;
    let advanceProgressInterval;

    // DOM elements
    const questionText = document.getElementById('question-text');
    const optionsContainer = document.getElementById('options-container');
    const currentQuestionElement = document.getElementById('current-question');
    const scoreElement = document.getElementById('score');
    const totalTimeElement = document.getElementById('total-time');
    const timerElement = document.getElementById('timer');
    const feedbackElement = document.getElementById('feedback');
    const resultContainer = document.getElementById('result-container');
    const finalScoreElement = document.getElementById('final-score');
    const resultMessageElement = document.getElementById('result-message');
    const correctCountElement = document.getElementById('correct-count');
    const incorrectCountElement = document.getElementById('incorrect-count');
    const timeTakenElement = document.getElementById('time-taken');
    const percentageElement = document.getElementById('percentage');
    const restartBtn = document.getElementById('restart-btn');
    const homeBtn = document.getElementById('home-btn');

    // Initialize the quiz
    function initQuiz() {
        currentQuestionIndex = 0;
        score = 0;
        userAnswers = [];
        quizCompleted = false;
        quizStartTime = Date.now();
        
        // Hide result container
        resultContainer.style.display = 'none';
        
        // Show quiz elements
        document.querySelector('.question-container').style.display = 'block';
        document.querySelector('.timer-container').style.display = 'block';
        
        // Update UI
        updateScore();
        updateQuestionCounter();
        updateTotalTime();
        
        // Load first question
        loadQuestion(currentQuestionIndex);
        
        // Start quiz timer
        startQuizTimer();
    }

    // Load a question
    function loadQuestion(index) {
        if (index >= gkQuestions.length) {
            endQuiz();
            return;
        }
        
        const question = gkQuestions[index];
        
        // Update question text
        questionText.textContent = question.question;
        
        // Clear options container
        optionsContainer.innerHTML = '';
        
        // Create option elements
        const optionLetters = ['A', 'B', 'C', 'D'];
        
        question.options.forEach((option, i) => {
            const optionElement = document.createElement('div');
            optionElement.className = 'option';
            optionElement.dataset.index = i;
            
            // Check if user has already answered this question
            if (userAnswers[index] !== undefined) {
                if (userAnswers[index] === i) {
                    optionElement.classList.add('selected');
                }
                if (i === question.correctAnswer) {
                    optionElement.classList.add('correct');
                } else if (userAnswers[index] === i && userAnswers[index] !== question.correctAnswer) {
                    optionElement.classList.add('incorrect');
                }
            }
            
            optionElement.innerHTML = `
                <div class="option-letter">${optionLetters[i]}</div>
                <div class="option-text">${option}</div>
            `;
            
            // Add click event if not already answered
            if (userAnswers[index] === undefined) {
                optionElement.addEventListener('click', () => selectOption(i));
            }
            
            optionsContainer.appendChild(optionElement);
        });
        
        // Update UI
        updateQuestionCounter();
        updateProgressBar(index + 1, gkQuestions.length);
        
        // Reset feedback
        feedbackElement.className = 'feedback';
        feedbackElement.textContent = '';
        
        // Start question timer
        startQuestionTimer();
    }

    // Select an option
    function selectOption(optionIndex) {
        // Prevent multiple selections
        if (userAnswers[currentQuestionIndex] !== undefined) return;
        
        // Mark the selected option
        const options = document.querySelectorAll('.option');
        options.forEach(option => {
            option.classList.remove('selected');
            option.style.pointerEvents = 'none'; // Disable further clicks
        });
        
        options[optionIndex].classList.add('selected');
        
        // Check answer
        const isCorrect = optionIndex === gkQuestions[currentQuestionIndex].correctAnswer;
        userAnswers[currentQuestionIndex] = optionIndex;
        
        if (isCorrect) {
            score++;
            updateScore();
            showFeedback(true);
            
            // Highlight correct answer
            options[gkQuestions[currentQuestionIndex].correctAnswer].classList.add('correct');
        } else {
            showFeedback(false, gkQuestions[currentQuestionIndex].options[gkQuestions[currentQuestionIndex].correctAnswer]);
            
            // Highlight correct and incorrect answers
            options[gkQuestions[currentQuestionIndex].correctAnswer].classList.add('correct');
            options[optionIndex].classList.add('incorrect');
        }
        
        // Stop question timer
        if (questionTimer && questionTimer.stopTimer) {
            questionTimer.stopTimer();
        }
        
        // Auto advance to next question after 2 seconds
        startAutoAdvance(2000); // 2 seconds
    }

    // Auto advance to next question
    function startAutoAdvance(duration) {
        // Create or show auto-advance progress bar
        let progressBar = document.querySelector('.auto-advance-progress');
        if (!progressBar) {
            progressBar = document.createElement('div');
            progressBar.className = 'auto-advance-progress';
            progressBar.innerHTML = '<div class="advance-progress"></div>';
            feedbackElement.parentNode.insertBefore(progressBar, feedbackElement.nextSibling);
        }
        
        const progressFill = progressBar.querySelector('.advance-progress');
        progressBar.classList.add('active');
        progressFill.style.width = '0%';
        
        // Clear any existing intervals
        if (autoAdvanceInterval) clearTimeout(autoAdvanceInterval);
        if (advanceProgressInterval) clearInterval(advanceProgressInterval);
        
        // Start progress bar animation
        let progress = 0;
        const increment = 100 / (duration / 50); // Update every 50ms
        
        advanceProgressInterval = setInterval(() => {
            progress += increment;
            progressFill.style.width = `${Math.min(progress, 100)}%`;
        }, 50);
        
        // Auto advance after duration
        autoAdvanceInterval = setTimeout(() => {
            progressBar.classList.remove('active');
            clearInterval(advanceProgressInterval);
            goToNextQuestion();
        }, duration);
    }

    // Go to next question
    function goToNextQuestion() {
        currentQuestionIndex++;
        
        if (currentQuestionIndex < gkQuestions.length) {
            loadQuestion(currentQuestionIndex);
        } else {
            endQuiz();
        }
    }

    // Start question timer (30 seconds)
    function startQuestionTimer() {
        // Stop previous timer if exists
        if (questionTimer && questionTimer.stopTimer) {
            questionTimer.stopTimer();
        }
        
        questionTimer = initTimer(60, onTimeUp);
        if (questionTimer) {
            questionTimer.startTimer();
        }
    }

    // Handle time up for a question
    function onTimeUp() {
        // Disable all options
        const options = document.querySelectorAll('.option');
        options.forEach(option => {
            option.style.pointerEvents = 'none';
        });
        
        // Mark the correct answer
        const correctIndex = gkQuestions[currentQuestionIndex].correctAnswer;
        options[correctIndex].classList.add('correct');
        
        // Show feedback
        showFeedback(false, gkQuestions[currentQuestionIndex].options[correctIndex]);
        
        // Auto advance to next question after 2 seconds
        startAutoAdvance(2000);
    }

    // Show feedback
    function showFeedback(isCorrect, correctAnswer = null) {
        // Update feedback message
        if (isCorrect) {
            feedbackElement.textContent = "Correct! 🎉";
            feedbackElement.className = 'feedback correct show';
            playSound('correct');
            createConfetti();
        } else {
            feedbackElement.textContent = correctAnswer ? 
                `Incorrect. Correct answer: ${correctAnswer}` : 
                "Time's up!";
            feedbackElement.className = 'feedback incorrect show';
            playSound('incorrect');
        }
    }

    // Start quiz timer (5 minutes total)
    function startQuizTimer() {
        let totalSeconds =1500; // 5 minutes
        
        const updateTimerDisplay = () => {
            totalTimeElement.textContent = formatTime(totalSeconds);
        };
        
        updateTimerDisplay();
        
        const timerInterval = setInterval(() => {
            if (quizCompleted) {
                clearInterval(timerInterval);
                return;
            }
            
            totalSeconds--;
            updateTimerDisplay();
            
            if (totalSeconds <= 0) {
                clearInterval(timerInterval);
                endQuiz();
            }
        }, 1000);
    }

    // Update question counter
    function updateQuestionCounter() {
        currentQuestionElement.textContent = `${currentQuestionIndex + 1}/${gkQuestions.length}`;
    }

    // Update score display
    function updateScore() {
        scoreElement.textContent = score;
    }

    // Update total time display
    function updateTotalTime() {
        totalTimeElement.textContent = "05:00";
    }

    // Update progress bar
    function updateProgressBar(current, total) {
        const progressBar = document.querySelector('.progress');
        if (progressBar) {
            const percentage = (current / total) * 100;
            progressBar.style.width = `${percentage}%`;
        }
    }

    // End the quiz
    function endQuiz() {
        quizCompleted = true;
        
        // Stop timers
        if (questionTimer && questionTimer.stopTimer) {
            questionTimer.stopTimer();
        }
        
        // Stop auto-advance
        if (autoAdvanceInterval) clearTimeout(autoAdvanceInterval);
        if (advanceProgressInterval) clearInterval(advanceProgressInterval);
        
        // Calculate quiz duration
        const quizDuration = Math.floor((Date.now() - quizStartTime) / 1000);
        
        // Calculate results
        const correctCount = score;
        const incorrectCount = gkQuestions.length - score;
        const percentage = Math.round((score / gkQuestions.length) * 100);
        
        // Update result display
        finalScoreElement.textContent = `${score}/${gkQuestions.length}`;
        correctCountElement.textContent = correctCount;
        incorrectCountElement.textContent = incorrectCount;
        timeTakenElement.textContent = formatTime(quizDuration);
        percentageElement.textContent = `${percentage}%`;
        
        // Set result message based on performance
        let message = "";
        if (percentage >= 90) {
            message = "Outstanding! You're a General Knowledge genius! 🎉";
        } else if (percentage >= 70) {
            message = "Excellent work! You have great knowledge! 👍";
        } else if (percentage >= 50) {
            message = "Good job! You know quite a bit! 👏";
        } else {
            message = "Keep learning! You'll do better next time! 💪";
        }
        resultMessageElement.textContent = message;
        
        // Show result container with animation
        document.querySelector('.question-container').style.display = 'none';
        document.querySelector('.timer-container').style.display = 'none';
        resultContainer.style.display = 'block';
        
        // Create confetti for good scores
        if (percentage >= 70) {
            createConfetti();
        }
    }

    // Event Listeners for result buttons
    restartBtn.addEventListener('click', function() {
        initQuiz();
    });

    homeBtn.addEventListener('click', function() {
        window.location.href = "index.html";
    });

    // Initialize the quiz when page loads
    initQuiz();

});
